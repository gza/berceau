DATABASE_ADMIN_USER=boss
DATABASE_ADMIN_PASSWORD=secret
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=berceau-dev
DATABASE_USER=berceau
DATABASE_PASSWORD=berceau-password

# Database URLs for Prisma (runtime and migrations)
DATABASE_URL="postgresql://berceau:berceau-password@127.0.0.1:5432/berceau-dev?schema=public"
MIGRATION_DATABASE_URL="postgresql://boss:secret@127.0.0.1:5432/berceau-dev?schema=public"

# Session secret for express-session (CSRF protection)
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=0000111122223333444455556666777788889999aaaabbbbccccddddeeeeffff


# Email / SMTP Configuration
# In development: use Mailpit (docker-compose.yml)
# In production: point to your SMTP provider
# Note: Use 'localhost' when running app/tests on host machine
#       Use 'mailpit' when running app inside docker containers
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_TLS_ENFORCED=false
SMTP_FROM_DEFAULT=noreply@example.com
TIMEOUT_CONNECT_MS=5000
TIMEOUT_SEND_MS=10000
